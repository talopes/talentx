<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>TalentX – Fase 1</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
  <style>
    .progress-bar-tx {
      background: linear-gradient(90deg, #3b82f6, #7c3aed);
    }
    .form-control, .form-select {
      background-color: #1e293b;
      border: 1px solid #334155;
      color: #f8fafc;
      border-radius: 10px;
      padding: 0.7rem;
    }
    .form-control:focus, .form-select:focus {
      border-color: #7c3aed;
      box-shadow: 0 0 0 0.15rem rgba(124, 58, 237, 0.25);
      background-color: #1e293b;
      color: #fff;
    }
    label {
      margin-bottom: 0.3rem;
      font-weight: 500;
    }
  </style>
</head>
<body class="bg-dark text-light" style="font-family: 'Poppins', sans-serif;">

  <div class="container py-4" style="max-width: 600px;">

    <!-- Barra de Progresso -->
    <div class="progress mb-4" style="height: 10px; border-radius: 8px;">
      <div class="progress-bar progress-bar-tx" role="progressbar" style="width: 25%;" aria-valuenow="25" aria-valuemin="0" aria-valuemax="100"></div>
    </div>

    <h2 class="text-info mb-2">🕹️ Fase 1 de 4: Crie seu avatar</h2>
    <p class="mb-4 text-light-emphasis">Vamos começar com o básico. Quem é você na fila do trampo?</p>

   <form action="/step1" method="POST" class="needs-validation" novalidate>

       <div class="mb-3">
        <label for="vaga" class="form-label">Tipo de vaga que você busca</label>
        <select class="form-select" id="vaga" name="vaga" required>
          <option value="">Selecione uma opção</option>
          <option value="Estágio">Estágio</option>
          <option value="Jovem Aprendiz">Jovem Aprendiz</option>
          <option value="CLT">CLT</option>
          <option value="Curso">Curso</option>
        </select>
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

      <div class="mb-3">
        <label for="nome" class="form-label">Nome completo</label>
        <input type="text" class="form-control" id="nome" name="nome" required>
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

      <div class="mb-3">
        <label for="nascimento" class="form-label">Data de nascimento</label>
        <input type="date" class="form-control" id="nascimento" name="nascimento" required>
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

<div class="mb-3">
  <label for="genero" class="form-label">Como você se identifica?</label>
  <select class="form-select" id="genero" name="genero" required>
    <option value="">Selecione</option>
    <option value="Mulher cisgênero">Mulher cisgênero</option>
    <option value="Homem cisgênero">Homem cisgênero</option>
    <option value="Mulher transgênero">Mulher transgênero</option>
    <option value="Homem transgênero">Homem transgênero</option>
    <option value="Pessoa não binária">Pessoa não binária</option>
    <option value="Prefiro não dizer">Prefiro não dizer</option>
    <option value="Outro">Outro</option>
  </select>
   <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
</div>

     
      
      <div class="mb-3">
        <label for="raca" class="form-label">Como você se identifica em relação à sua raça/cor?</label>
        <select class="form-select" id="raca" name="raca" required>
          <option value="">Selecione uma opção</option>
          <option value="Preta">Preta</option>
          <option value="Parda">Parda</option>
          <option value="Branca">Branca</option>
          <option value="Amarela">Amarela</option>
          <option value="Indígena">Indígena</option>
          <option value="Prefiro não dizer">Prefiro não dizer</option>
        </select>
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

      <div class="mb-3">
        <label for="telefone" class="form-label">Telefone com WhatsApp</label>
        <input type="tel" class="form-control" id="telefone" name="telefone" required>
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

      <div class="mb-3">
        <label for="email" class="form-label">E-mail</label>
        <input type="email" class="form-control" id="email" name="email">
         <div class="invalid-feedback">
          Ops, faltou alguma coisa! 
        </div>
      </div>

      <div class="mb-3">
  <label for="cep" class="form-label">CEP</label>
  <div class="input-group">
    <input type="text" class="form-control" id="cep" name="cep" required>
    <button type="button" class="btn btn-outline-light" id="buscar-cep">
      🔍
    </button>
  </div>
  <small class="form-text text-info mt-1">Digite o CEP e clique na lupa para preencher bairro, cidade e estado automaticamente.</small>
  <div class="invalid-feedback">Ops, faltou alguma coisa!</div>
</div>



      <div class="mb-3">
        <label for="bairro" class="form-label">Bairro</label>
        <input type="text" class="form-control" id="bairro" name="bairro" readonly>
      </div>

      <div class="mb-3">
        <label for="cidade" class="form-label">Cidade</label>
        <input type="text" class="form-control" id="cidade" name="cidade" readonly>
      </div>

      <div class="mb-3">
        <label for="estado" class="form-label">Estado</label>
        <input type="text" class="form-control" id="estado" name="estado" readonly>
      </div>


      <div class="form-check text-start mb-4">
  <input class="form-check-input" type="checkbox" value="" id="lgpdCheck" required>
  <label class="form-check-label" for="lgpdCheck">
    Estou ciente de que o TalentX poderá usar, armazenar e compartilhar meus dados com empresas parceiras para fins de empregabilidade.  
    Caso deseje excluir meus dados futuramente, posso solicitar via DM no Instagram <strong>@talentx.curriculo</strong>.
  </label>
  <div class="invalid-feedback">
    Você precisa aceitar os termos para continuar.
  </div>
</div>

<div class="text-center mt-4">
  <button type="submit" class="btn btn-start-missao">Próxima Fase</button>
</div>

    </form>

    <div class="text-center mt-4">
  <a href="/intro" class="btn btn-secondary" style="background-color: #334155; color: #fff; border-radius: 10px; padding: 10px 24px; font-weight: 600;">
    🔁 Voltar ao Início
  </a>
</div>
  </div>
<div class="text-center mt-4">
  <a href="/missao" class="btn btn-outline-light" style="border-radius: 10px; padding: 10px 24px; font-weight: 500;">
    📖 Ver nossa missão
  </a>
</div>

 <script>
  const cepInput = document.getElementById("cep");
  const bairroInput = document.getElementById("bairro");
  const cidadeInput = document.getElementById("cidade");
  const estadoInput = document.getElementById("estado");
  const buscarBtn = document.getElementById("buscar-cep");

  function buscarEndereco(cep) {
    const cleanCep = cep.replace(/\D/g, "");
    if (cleanCep.length === 8) {
      return fetch(`https://viacep.com.br/ws/${cleanCep}/json/`)
        .then(response => response.json())
        .then(data => {
          if (!data.erro) {
            bairroInput.value = data.bairro || "";
            cidadeInput.value = data.localidade || "";
            estadoInput.value = data.uf || "";
          } else {
            alert("CEP inválido.");
          }
        });
    } else {
      alert("Você precisa preencher os dados antes de avançar na missão!");
    }
  }

  // Botão lupa
  buscarBtn.addEventListener("click", () => {
    buscarEndereco(cepInput.value);
  });

  // Bloquear Enter de enviar o formulário automaticamente
  document.querySelector("form").addEventListener("keydown", (e) => {
    if (e.key === "Enter") {
      e.preventDefault();
    }
  });

  // Validação completa ao enviar
  (() => {
    'use strict';
    const forms = document.querySelectorAll('.needs-validation');
    Array.from(forms).forEach(form => {
      form.addEventListener('submit', async event => {
        await buscarEndereco(cepInput.value);

        if (!form.checkValidity() ||
            bairroInput.value === "" ||
            cidadeInput.value === "" ||
            estadoInput.value === "") {
          event.preventDefault();
          event.stopPropagation();
          alert("Preencha todos os campos obrigatórios e clique na lupa para buscar o endereço.");
        }

        form.classList.add('was-validated');
      }, false);
    });
  })();
</script>



</body>
</html>
