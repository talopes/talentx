<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Missão em Progresso</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #0f172a;
      color: #f1f5f9;
      text-align: center;
      font-family: 'Poppins', sans-serif;
      padding: 40px 20px;
    }
    .progress-circle {
      width: 160px;
      height: 160px;
      border-radius: 50%;
      border: 12px solid #3b82f6;
      border-top-color: #9333ea;
      margin: 0 auto 20px;
      animation: spin 2s linear infinite;
    }
    @keyframes spin {
      0% { transform: rotate(0deg);}
      100% { transform: rotate(360deg);}
    }
    .percent {
      font-size: 3rem;
      font-weight: bold;
      color: #22d3ee;
    }
    .btn-next {
      background: linear-gradient(90deg, #3b82f6, #9333ea);
      border: none;
      padding: 14px 28px;
      font-size: 1rem;
      font-weight: bold;
      border-radius: 12px;
      margin-top: 30px;
      animation: pulse 1.5s infinite;
      color: white;
    }
    @keyframes pulse {
      0% { transform: scale(1); box-shadow: 0 0 10px rgba(59, 130, 246, 0.4);}
      50% { transform: scale(1.05); box-shadow: 0 0 24px rgba(124, 58, 237, 0.8);}
      100% { transform: scale(1); box-shadow: 0 0 10px rgba(59, 130, 246, 0.4);}
    }
  </style>
</head>
<body>
  <div class="progress-circle"></div>
  <div class="percent">50%</div>
  <h2 class="mt-3">Você concluiu metade da missão! 🚀</h2>
  <p class="mb-4">Faltam só mais duas etapas para você conquistar seu currículo profissional.</p>
  <a href="/step3" class="btn btn-next">Continuar</a>
</body>
</html>
